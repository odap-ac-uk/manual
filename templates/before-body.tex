\definecolor{odapred}{RGB}{231,42,42}

\pagestyle{fancy}
\fancyhead{} % clear all header fields
$if(head.img)$
\fancyhead[LO,LE]{\includegraphics[height=1cm]{$head.img$}}
$endif$
$if(title)$
\fancyhead[RO,RE]{\color{odapred}$title$}
$endif$

%\allsectionsfont{\color{odapred}}
\sectionfont{\color{odapred}\sectionrule{3ex}{2pt}{-1ex}{0.5pt}} % above and below

$if(has-frontmatter)$
\frontmatter
$endif$
$if(title)$
$if(beamer)$
\frame{\titlepage}
$else$
{\color{odapred}\maketitle}
$endif$
$if(abstract)$
\begin{abstract}
$abstract$
\end{abstract}
$endif$
$endif$
